/**
 * Shared atmosphere paint tokens. Backdrop particles and the price-ruler
 * marker must stay on the same two colors — and the same opacity — so a move
 * "up" reads the same whether it is a floating cell or the ruler tip.
 */
export const LOGIN_PARTICLE_UP = "#fff0c2";
export const LOGIN_PARTICLE_DOWN = "#d9a63e";

/** Canvas / marker opacity on the amber field. Solid cream at 1.0 washes out. */
export const LOGIN_PARTICLE_ALPHA = 0.46;
