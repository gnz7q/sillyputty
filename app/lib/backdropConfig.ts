/** Which instrument the lights-off atmosphere reads, and what "1 unit" means. */

export const DEFAULT_BACKDROP_INST_ID = "BTC-USDT-SWAP";

/** Lots ≈ 1 BTC given OKX ctVal 0.01 on BTC-USDT-SWAP. */
export const DEFAULT_BACKDROP_REFERENCE_LOTS = 100;
